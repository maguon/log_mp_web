import { createAction } from 'redux-actions';
export const getCityList = createAction('GET_CITY_LIST');
export const enquiryModal = createAction('ENQUIRY_MODAL');
export const enquiryFreight = createAction('ENQUIRY_FREIGHT');
